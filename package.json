{
  "name": "text_ai",
  "version": "1.0.0",
  "description": "Text AI and NLP coursework",
  "scripts": {
    "start": "bundle exec jekyll serve --port 4001 --watch",
    "build": "bundle exec jekyll build"
  },
  "dependencies": {
    "bootstrap": "^5.3.0-alpha1",
    "github-markdown-css": "^5.2.0"
  }
}
